1. JSX란 무엇인가?
  - 자바스크립트 안에 있는 HTML 코드를 말함
  - JSX = JavaScript XML => 왜냐하면... HTML은 XML이라고 할 수 있기 때문임
  
  (장점)
    - 브라우저에서 잘 작동한다.
    - 작성하기 쉽다.
  
  (예시)
    function App() {
      return (
        <div>
          <h2>Let's get started!</h2>
        </div>
      );
    }
    export default App;
    
  (확인)
    크롬 > 개발자 검사(F12) > source 탭에 가면 페이지의 일부로 다운  된 항목 확인 가능인데
    리액트 변환 코드 + 작성한 코드가 섞여져서 전달되고 있음을 확인 가능
    
