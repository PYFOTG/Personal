1. 전체 구축 프로세스가 CSS 파일을 인식하도록 선언하기
  - 인식 > CSS 코드 검토 > 완성된 응용프로그램에 삽입
  -✨ CSS가 적용되길 바라는 파일에 import './ExpenseItem.css'; 구문을 최상단에 선언해주면 됨
  - 위 구문은, 구축 프로그램에 css 파일을 고려해야한다는것을 전달하는 구문임

2. React사용 시 주의점
  - React를 사용한 <div>태그와 같은 내용물은 html이 아닌, JSX구문이므로 <div class = />사용하면 오류난다!
  -✨ JSX 문법에서는 <div className = ""> 과 같이 사용해야함
